
const {v4: id} = require('uuid')
const db = require('../db')

class CatsRepository {
    // Пока Конструктор не нужен
    // constructor(){}

    getAll() {
        return {}
    }
    getById({id}) {
        return {}
    }
    create(body) {
        return {}
    }
    update({id}, body) {
        return {}
    }
    remove({id}) {
        return {}
    }
}

module.exports = CatsRepository 